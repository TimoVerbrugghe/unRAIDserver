[Unit]
Description = Ubuntu Update
After=network-online.target

[Service]
Type=oneshot
ExecStart=/usr/bin/apt-get update && /usr/bin/apt-get upgrade -y && /usr/bin/apt-get dist-upgrade -y && /usr/bin/apt-get autoremove -y

[Install]
WantedBy=multi-user.target
